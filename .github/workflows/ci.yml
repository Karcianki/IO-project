name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: yarn install
      run: |
        yarn install
    - name: validate CSS
      run: |
        for f in $(find . -name 'src/*.css'); do
          yarn run stylelint $f
        done;
    - name: install pytest
      run: |
        python -m pip install pytest
    - uses: browser-actions/setup-firefox@v1
    - name: run tests
      run: |
        yarn run python -m pytest tests --capture=sys