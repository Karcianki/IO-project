name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: yarn install
      run: |
        yarn install
    - name: validate CSS
      run: |
        for f in $(find . -name 'src/*.css'); do
          yarn run stylelint $f
        done;
    - name: validate HTML
      run: |
        for f in $(find . -name 'src/*.html'); do
          yarn run html-validate $f
        done;
    - name: install dependencies
      run: |
        pip install pytest
        pip install selenium
        pip install webdriver_manager
    - uses: browser-actions/setup-firefox@v1
    - name: run tests
      run: |
        yarn test
